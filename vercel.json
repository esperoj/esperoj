{
  "buildCommand": "pip install poetry && poetry install --with build,cli,server && poetry run pdoc --docformat google --output-directory public/docs esperoj",
  "outputDirectory": "public",
  "regions": ["fra1"]
}