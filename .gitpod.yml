image: ghcr.io/esperoj/esperoj-dev

tasks:
    - name: Setup
      before: |
        chezmoi init --apply --force
      init: |
        source ~/.profile
        rm -rf .venv
        python -m venv .venv
        source .venv/bin/activate
        poetry install --with build,cli,dev,server,test
      command: |
        source ~/.profile
        source .venv/bin/activate
        start.sh pcloud caddy
        task dev-server

ports:
    - port: 20711
      name: Caddy
      onOpen: open-browser
      description: Caddy
      visibility: private
      protocol: https